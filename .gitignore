.env
.env.local
.env.*.local
.env.production
**/.env.production
node_modules
dist
.DS_Store
.vite
*.local
*.tsbuildinfo
vite.config.js
vite.config.d.ts
*.pem
*.key

# Real lab reports used to tune the ingest parser — PHI, never commit.
ocr-sample/

# AWS Lightsail staging env + provisioning secrets (never commit)
.env.staging
**/.env.staging
/outputs/aws-setup/

# Playwright sessions contain live authentication cookies. Generate them
# locally from the staging password manager and never commit them.
/.auth/
/playwright-report/
/test-results/
